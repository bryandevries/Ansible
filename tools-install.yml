---
  - name: Install Neofetch and htop
    hosts: linux
    become: true
    tasks:

      - name: Update apt cache
        apt:
          update_cache: yes

      - name: Install Neofetch
        apt:
          name: 
            - neofetch
            - net-tools
            - htop
          state: latest
